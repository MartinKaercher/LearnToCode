#include <stdio.h>


double MAS()
{
    // This should take a vector or cube as input and just return the evaluated MAS kernel as an array
    printf("Do stuff")
    // It should also take a string as argument which decides which MAS is used
}


int main()
{
    return 0;
}
